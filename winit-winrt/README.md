# winit-winrt

This crate provides Winit's WinRT/UWP backend. It is selected by compiling with
`cfg(__WINRT__)` and targets `x86_64-pc-windows-gnu`.

See `winit/docs/winrt.md` for build instructions and platform limitations.
